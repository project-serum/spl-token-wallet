import React from 'react';
import { StyledImg } from './NavigationFrame';
import Discord from '../../images/violetDiscord.svg';

const DiscordIcon = () => {
  return <StyledImg src={Discord} alt="discordIcon" />;
};

export default DiscordIcon;
